export class Projectile {
  constructor(x, y, type, size, speed, angle) {
    this.x = x;
    this.y = y;
    this.type = type;
    this.size = size;
    this.speed = speed;
    this.angle = angle;
  }
}
